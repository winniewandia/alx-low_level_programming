#include "main.h"

/**
 * _abs - Prints absolute value of a number
 * @num: the value to be changed to absolute
 *
 * Return: Absolute value
 */

int _abs(int num)
{
	if (num < 0)
	{
		num = -1 * num;
		return (num);
	}
	else
	{
		return (num);
	}
}

