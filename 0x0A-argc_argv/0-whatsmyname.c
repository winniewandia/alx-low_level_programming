#include "main.h"
#include <stdio.h>

/**
 * main - Used tp print a program's name
 * @argc: Number of arguments
 * @argv: Array of pointers to arguments
 *
 * Return: Always 0 success
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

